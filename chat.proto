syntax = "proto3";
package chat;

message Message {
  string nombre_planeta = 1;
  string nombre_ciudad = 2;
  int32 cantidad_soldados = 3;
}

service ChatService {
  rpc SayHello(Message) returns (Message) {}
}